import { Worker } from "@/app/types/User";

export default function DoctorsTypeList({
  currentJob,
}: {
  currentJob: Worker;
}) {
  return (
    <div>
      <p>Just a type List</p>
    </div>
  );
}
